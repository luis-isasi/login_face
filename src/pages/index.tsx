const Home = () => {
  return (
    <video
      autoPlay
      // src={}
      muted={false}
      playsInline
      // ref={(ref) => {
      //   this.video = ref;
      // }}
      className="w-96 h-96"
    />
  );
};

export default Home;
